NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Optional: enables LLM meeting extraction (OpenAI). Without this, heuristic fallback is used.
OPENAI_API_KEY=

# Server-only: Postgres connection string for in-app migrations (/admin/setup).
# Supabase → Project Settings → Database → Connection string (URI). Do not expose to the browser.
SUPABASE_DB_URL=
